# Runtime dependencies
torch>=2.1
torchvision>=0.16
ultralytics>=8.0
# numpy<2 required: torch 2.x was compiled against numpy 1.x
numpy>=1.24,<2
scipy>=1.11
opencv-python-headless>=4.8
filterpy>=1.4
pyyaml>=6.0

# torchreid build prerequisites (install before torchreid)
Cython>=3.0
tensorboard>=2.0

# torchreid must be installed from source (not on PyPI):
#   pip install --no-build-isolation \
#       git+https://github.com/KaiyangZhou/deep-person-reid.git

# Dev / test
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4
gdown>=5.0
